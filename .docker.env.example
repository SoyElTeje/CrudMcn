# ============================================
# Variables de Entorno para Docker
# ============================================
# Copia este archivo a .docker.env y configura los valores reales
# Uso: docker-compose --env-file .docker.env up

# ============================================
# Base de Datos
# ============================================
DB_SERVER=tu-servidor-sql
DB_PORT=1433
DB_USER=tu-usuario
DB_PASSWORD=tu-contraseña-segura
DB_DATABASE=APPDATA

# Contraseña SA para SQL Server (si usas contenedor SQL Server)
DB_SA_PASSWORD=tu-contraseña-sa-segura

# ============================================
# Aplicación
# ============================================
APP_PORT=3001
PORT=3001
NODE_ENV=production

# ============================================
# Frontend (para build)
# ============================================
# URL donde estará disponible el backend
# IMPORTANTE: Debe ser accesible desde el navegador del usuario
# Si usas Docker, puede ser la IP del servidor o un dominio
VITE_API_BASE_URL=http://localhost:3001

# ============================================
# CORS
# ============================================
# Orígenes permitidos (separados por coma)
# Ejemplo: http://localhost:5173,https://tudominio.com
CORS_ORIGIN=*

# ============================================
# JWT (OBLIGATORIO)
# ============================================
# Genera un secret seguro: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=GENERAR_SECRET_SUPER_SEGURO_Y_LARGO_AQUI
JWT_EXPIRES_IN=24h

# ============================================
# Logging
# ============================================
LOG_LEVEL=info

# ============================================
# Trial (opcional)
# ============================================
TRIAL_DB=
TRIAL_TABLE=

