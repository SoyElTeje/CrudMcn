# Database Configuration
DB_SERVER=
DB_PORT=
DB_USER=
DB_PASSWORD=
DB_DATABASE=

# Trial Database and Table (for testing)
TRIAL_DB=
TRIAL_TABLE=

# Server Configuration
PORT=
NODE_ENV=

# CORS Configuration
CORS_ORIGIN=*

# JWT Configuration (for future authentication)
JWT_SECRET=
JWT_EXPIRES_IN=

# Logging Configuration
LOG_LEVEL=

# Frontend Configuration
# URL base del backend API (OBLIGATORIO - sin esto la app no funcionará)
# Ejemplo para desarrollo local: http://localhost:3001
# Ejemplo para red local: http://192.168.168.209:3001
# Ejemplo para producción: https://api.tudominio.com
VITE_API_BASE_URL=

# NOTA: Las siguientes variables son OPCIONALES y solo necesarias en casos específicos:
# - VITE_PORT: Solo si necesitas cambiar el puerto de desarrollo (por defecto: 5173)
# - VITE_PREVIEW_PORT: Solo si necesitas cambiar el puerto de preview (por defecto: 4173)
# - VITE_HOST: Solo si necesitas cambiar el host (por defecto: 0.0.0.0)
# En la mayoría de los casos NO necesitas configurarlas
